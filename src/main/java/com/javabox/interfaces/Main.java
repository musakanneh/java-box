package com.javabox.interfaces;

/**
 * interfaces:
 * - used to build loosely-coupled, extensible, testable apps
 * - it's a type, similar to a class, but it only includes method declarations
 *
 * interface (what should be done) vs class (how it should be done)
 *
 * coupling => level of dependency between two software entities - like classes
 */

public class Main {
    public static void main(String[] args) {
        System.out.println();
    }
}
